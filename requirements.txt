# Core storage and API
lancedb~=0.14.0
pyarrow~=17.0
numpy>=1.26,<3
fastapi>=0.115
uvicorn[standard]>=0.32
gunicorn>=23.0
pydantic>=2.9
pydantic-settings>=2.4
python-multipart>=0.0.9      # multipart uploads (reverse image / face search)

# Imaging
pillow>=11.0
pillow-heif>=0.20            # HEIC/HEIF, replaces the unmaintained pyheif
ExifRead>=3.0                # EXIF for RAW files Pillow cannot open
tqdm>=4.66

# Embeddings — SigLIP 2 by default (Apache-2.0, better retrieval than CLIP).
# Install the CPU-only torch build if you have no GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4
transformers>=4.49           # 4.49 is the first release with SigLIP 2

# Faces — InsightFace (RetinaFace detection + ArcFace recognition).
insightface>=0.7.3
onnxruntime>=1.18            # swap for onnxruntime-gpu on an NVIDIA machine

# Optional extras
# open-clip-torch>=2.24      # PHOTO_EMBED_BACKEND=open_clip
# face-recognition>=1.3      # PHOTO_FACE_BACKEND=dlib fallback
